---
id: bug-reporting
title: Ein Problem melden
section: Verbinden & erweitern
order: 360
summary: "Stimmt etwas an der Kasse nicht? Senden Sie einen Bericht — getippt, gesprochen oder mit Bildschirmaufzeichnung — von jedem Bildschirm aus über die 🐞-Schaltfläche."
routes: [/report-issue]
keywords: [fehler, problem, bericht, support, aufzeichnung, sprache]
---

# Ein Problem melden

Stimmt etwas an der Kasse nicht? Senden Sie einen Bericht — getippt, gesprochen oder mit Bildschirmaufzeichnung — von jedem Bildschirm aus über die 🐞-Schaltfläche.

## Verwendung

1. Klicken Sie auf die 🐞-Schaltfläche im linken Menü (nur Manager und Admins). Ein kleines Fenster öffnet sich in der Ecke — die Kasse darunter läuft weiter, sodass Sie das Problem reproduzieren können, während Sie es beschreiben. Verdeckt es etwas, das Sie sehen müssen, ziehen Sie es an der Titelleiste an eine andere Stelle.
2. Tippen Sie, was schiefgelaufen ist, oder nehmen Sie eine Sprachnotiz auf; Sie können auch Ihren Bildschirm aufzeichnen oder einen Screenshot machen — drücken Sie "Screenshot aufnehmen", um ein Standbild des aktuellen Bildschirms anzuhängen (mehrere sind möglich). Die letzten Warn- und Fehlerprotokolle der Kasse werden automatisch angehängt.
3. Drücken Sie „Bericht speichern“. Der Bericht wird sofort auf der Kasse gespeichert — auch offline — und lädt automatisch in die Cloud hoch, sobald das Geschäft wieder online ist.
4. Das Panel bleibt beim Wechsel zwischen Bildschirmen bei Ihnen — mit allem, was darin ist. Machen Sie Screenshots auf mehreren Seiten, lassen Sie eine Aufzeichnung laufen, während Sie das Problem zeigen, und drücken Sie dann „Bericht speichern“ (oder „Verwerfen“, um den Entwurf zu löschen). Das Panel schließt sich nie von selbst; ✕ blendet es nur aus, und 🐞 holt es mit Ihrem Entwurf zurück. Wird eine Seite während der Aufzeichnung vollständig neu geladen (z. B. nach einem Sprachwechsel), stoppt die Aufzeichnung an dieser Stelle, und das bis dahin Aufgenommene bleibt erhalten.

Sie erreichen dies auch über das ☰-Menü über die Kachel Problem melden.

In der Android-Kasse-App erfasst "Screenshot aufnehmen" direkt, was die App gerade anzeigt — keine Berechtigungsabfrage. Auch die Sprachnotiz funktioniert: Beim ersten Aufnehmen fragt Android nach Mikrofonzugriff; einmal erlaubt, starten spätere Aufnahmen sofort (bei Ablehnung wieder aktivieren unter Telefon-Einstellungen → Apps → Universal Till → Berechtigungen). Bildschirmaufzeichnung und das Fotografieren mit der Kamera sind in der Android-App noch nicht verfügbar.

Mit ✕ wird das Panel ausgeblendet — Ihr Entwurf bleibt erhalten — bis Sie wieder 🐞 drücken: Es bleibt beim Wechsel zwischen Bildschirmen geschlossen, und die Seite „Problem melden“ öffnet es nicht mehr von selbst. „Verwerfen“ ist die Schaltfläche, die einen Entwurf tatsächlich löscht (mit Rückfrage). Ein Entwurf gilt nur für die aktuelle Kassensitzung; in einen Bericht passen bis zu 12 Screenshots.

Möchten Sie wissen, was aus einem gesendeten Bericht geworden ist? Siehe [Meine Berichte](/help/my-reports) — jeder von dieser Kasse gesendete Bericht mit seinem aktuellen Status.

Ist beim Senden eines Berichts der Diagnosemodus eingeschaltet (siehe Einstellungen → Diagnosemodus unter [Sprachen & Anzeige](/help/display)), enthält der Bericht zusätzlich einen Verweis auf diese Diagnosesitzung und ihre letzten technischen Ereignisse, damit der Support den Bericht neben dem zugehörigen Datenstrom lesen kann. Bei ausgeschaltetem Diagnosemodus wird nichts Zusätzliches gesendet.
