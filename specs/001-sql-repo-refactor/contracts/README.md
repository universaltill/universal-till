# Contracts: SQL Access Refactor to Data Repos

No new external API or protocol contracts are introduced in this feature. Existing handlers and plugin contracts remain unchanged; only data access is relocated into repository files under `internal/data/`.
