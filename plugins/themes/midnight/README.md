# Midnight Theme

Dark UI theme plugin for Universal Till. Style-only: it re-skins the POS with a
low-glare dark palette and keeps the default screen layout.

Asset-only plugin (`runtime: "none"`, no executable). Registers one
`type: "theme"` entry (`key: midnight`); after install, select it under
Settings → Theme. Package with `scripts/package-themes.sh`.
