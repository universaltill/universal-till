# Code review — help guide refresh (2026-07-18)

Branch `docs/help-guide-refresh`. The feature guide promised to stay in sync
with the product; tonight's ships made six entries stale. Refreshed ×4
locales: inventory (days-left + reorder suggestions), reports (the full new
card set), payments (preferred method + fee hints), plugins (trust badges +
consent), claim (QR by phone), catalog (cost prices, photos, per-variant
labels). Strings only — no template/logic changes; guard + help render test
green.
