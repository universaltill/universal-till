# Turkey market-entry research pass (PR #736)

**Date:** 2026-09-03
**Author:** pouria-teimouri
**Reviewer:** Scrum Master pipeline (independent pass, `lane:cloud-54`)
**Scope:** `docs/arch/turkey-fiscal-compliance.md` only — docs, no code.

## What changed

Replaces the stale 2026-07-16 backlog note in
`docs/arch/turkey-fiscal-compliance.md` with a 2026-09-02 research pass on
Turkey market entry: fiscal-device law (Law 3100, VUK 426/483/507/566/593),
the GMP-3 external-software integration route (wired "kasa modu" vs TSM
wireless mode), 2026 invoice/e-Arşiv thresholds and penalties, KVKK/VERBİS,
Law 805 Turkish-language duty, device-maker market shares (Token/Beko,
Pavo, Hugin, Ingenico/Worldline), and a competitor table (Adisyo, Simpra,
robotPOS, SambaPOS, etc. — confirms Square/SumUp/Zettle are absent from
Turkey). Closes with a business/legal and engineering checklist.

## Review

- **Scope discipline**: touches only `docs/arch/turkey-fiscal-compliance.md`
  — no code, no locale, no help topic, so none of the CI-blocking guards
  (`guard-i18n.sh`, `guard-compliance-claims.sh`, `guard-help-topics.sh`,
  etc.) apply to this diff, and none of them fired.
- **No compliance-outcome claims** (ADR-0040 territory even though the
  guard's path scope doesn't reach `docs/arch/`): the doc is explicit that
  it is "research pass... Not legal or tax advice", flags every
  unconfirmed item `(confirm)`, and never asserts we file anything on the
  merchant's behalf or that any device/route is "certified" by us — it
  describes what the *device maker's* certification requires, which is
  accurate scoping, not a claim about our own product.
- **No PII, no real shop/customer data, no secrets** — pilot-shop specifics
  are left as open questions (`ut-docs#1213`), not asserted here.
- **Consistent with standing decisions**: doesn't contradict any ADR;
  aligns with ADR-0025's device-partner fiscalization pattern (Germany TSE)
  by proposing the same shape for Turkey (till computes the sale, a
  certified ÖKC device issues the fiscal receipt) rather than proposing we
  self-certify.
- **Points at, doesn't duplicate, the canonical register**
  (`ut-docs/reference/turkey-compliance.md`) — the doc says so explicitly
  and defers to it on overlap, avoiding a second source of truth.
- **Confidence/sourcing is honest**: a dedicated "Confidence and gaps"
  section flags that GİB's primary texts were egress-blocked from the
  research sandbox and findings rest on secondary sources — appropriate
  for a research doc, not a decision record.

## CI

All 6 checks green on the head commit (`fb28619`): `desktop-shell`, `e2e`,
`contract`, `authors`, `build`, `playwright`. Docs-only diff, so this is
expected — no build/runtime surface was touched.

## Verdict

Approved. Merging via `merge` (not squash/rebase) per this pipeline's
merge-method convention (ut-docs#250) — GitHub's squash/rebase API
re-attributes commit content to the merging account's own email regardless
of the branch's actual commit author.

---
_Generated by [Claude Code](https://claude.ai/code)_
