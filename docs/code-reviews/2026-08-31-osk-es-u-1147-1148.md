# Code review: OSK ü (es) + ¿/¡ (sym) — ut-docs#1147, ut-docs#1148

**Branch:** `fix/1147-osk-es-u` · **Reviewer:** self-reviewed (two-line,
complexity:easy follow-ups from #1047's review; picked up mid-flight —
Farshid had already written the #1148 e2e test, source changes and the
rest of the coverage were missing)

## Change
- `web/public/osk.js`: append `ü` to the `es` layout's top row; add `¿`/`¡`
  to the shared `sym` layer next to `!`/`?`.
- `e2e/tests/osk-central-guard.spec.ts`: extended the existing es-layout
  row assertion to expect `ü`, added a real `pingüino`-typing check.

## Verification
- `npx playwright test tests/osk-central-guard.spec.ts` — 13/13 pass,
  including both new/extended assertions.
- `gofmt -l .` — clean. `go build ./...` — clean.
- `scripts/ci/guard-i18n.sh`, `scripts/ci/guard-compliance-claims.sh` —
  both pass (no new user-facing strings, no compliance wording touched).

## Risk
Minimal — pure data-table addition to an existing, well-tested layout
mechanism; no dead-key/accent logic changed. `toLocaleUpperCase` already
handles `ü → Ü` with no special-casing (same as the other accented
vowels already on this layout).
