# Code review — margins: cost-price capture + report (2026-07-17)

Branch `feat/margins`. Owner-reports increment: margins were blocked on
nothing capturing what the shop PAYS (items.cost_price existed, unused).

- Catalog detail panel (aside) gains a **Cost price** field: decimal major
  units in the form, stored minor via `SetItemCostPrice` (money-boundary
  rule; math.Round×100), panel re-renders with the saved value
  (`ItemCostPrice` getter).
- `POSRepo.MarginByItem`: per-item revenue − qty×cost over the window,
  variant cost preferred over item cost (COALESCE), **lines with no known
  cost excluded** — honest numbers only, no guessed margins.
- Reports page **Margins** card: revenue / −cost / margin (negative margins
  flagged red); empty-state explains to set cost prices in the catalog.
  i18n ×4 incl. the catalog field.

Tests extend the product-reports test: cost roundtrip + margin 100−40=60
for the seeded sale. All suites + guards green.

Follow-up: variant-level cost editing in the variant grid (item-level field
covers the common case); year-over-year + tax summary reports remain.
