# Code review: remove 4 dead locale keys (ut-docs#1759)

- **Date**: 2026-09-08
- **Branch**: `worktree-fix-1759-dead-locale-keys` (pushed as
  `fix/1759-dead-locale-keys`)
- **Card**: ut-docs#1759

## What shipped

`issuereport.my_reports.view_on_github`, `sync.chip_quarantined`,
`sync.chip_queued`, and `sync.chip_tills` were added to
`web/locales/en.json` by universal-till#750 but never referenced by any
template (`web/ui/**`) or Go code path — the sync-chip partial uses only
the ICU-plural `_one`/`_other` (and, for tills, `_title`/`_stale_title`)
variants of these keys, never the bare form. Removed all four from all
four shipped locales (`en`/`ar`/`fa`/`tr`), keeping the key sets
identical across locales (`guard-i18n.sh`'s own requirement).

`internal/pages/translations_page_test.go` used the bare
`sync.chip_queued` as its stand-in "a key that certainly exists" for two
tests (`TestTranslationsTable_FiltersByQuery`,
`TestTranslationsSet_RequiresManager`) — both updated to use
`sync.chip_offline` instead, a key that is genuinely rendered
(`web/ui/partials/sync_chip.html`).

**Notable finding from investigation, not just the original issue's
claim**: `issuereport.my_reports.view_on_github` had already been removed
once before, in commit `8e90b8f` ("i18n: drop the now-unreferenced
view_on_github locale key", 2026-09-07), but was accidentally
reintroduced by a later merge commit `b6bc9472` ("merge: bring PR #750 up
to current main") — that merge combined a 4-day-stale branch's own old
copy of the locale files with `main`'s newer state, and picked up the
stale branch's still-present copy of the already-deleted key instead of
`main`'s deletion. This diff removes it again; nothing about this
specific removal is expected to recur since #750 (the branch that carried
the stale copy) is already merged and no longer diverging.

## Independent review

A fresh-context Sonnet subagent (isolated worktree, no prior reasoning
carried over) reviewed cold. It:

- Independently re-grepped the whole repo for all 4 bare keys (not
  trusting the issue's or this diff's own claim) — confirmed zero
  references in any template or Go file; the `_one`/`_other`/`_title`
  suffixed siblings are untouched and remain live.
- Confirmed `httpx.T()` does no bare-key pluralization fallback, so
  removing the bare form cannot silently break the suffixed lookups.
- Ran `gofmt -l .`, `go build ./...`, `go vet ./...` — all clean.
- Ran the full `go test ./...` — every package `ok`, nothing skipped.
- Ran `guard-i18n.sh`, `guard-compliance-claims.sh`, `guard-help-topics.sh`
  — all green.
- Independently verified (Python, not just trusting the guard) that all
  four locale files are valid JSON with identical key sets (2058 keys
  each) and that all 4 target keys are absent from all 4 files.
- Independently traced the `8e90b8f` → `b6bc9472` reintroduction history
  by diffing both merge parents directly (`git show <parent>:web/locales/
  en.json | grep -c view_on_github`) rather than trusting the narrative —
  confirmed exactly as described above.
- Checked for the two recurring bug classes (missing `os.MkdirAll`, a
  cwd-relative path instead of `paths.Data(...)`) — not applicable, no
  file writes in this diff.
- Checked for real client/shop names or secret-shaped literals — none.
- Confirmed no `web/help/**` manual update is needed — these keys were
  never shown to a shop owner, so there is nothing to update.

**Verdict: SAFE TO MERGE** — no findings, no nitpicks.

## What was verified beyond automated tests

- All four locale JSON files parse and have identical key sets after the
  removal (both `guard-i18n.sh` and an independent Python check).
- The two edited tests were read in full (not just re-run) to confirm
  `sync.chip_offline` is a meaningful stand-in, not just a key that
  happens not to error.

## Deferred / out of scope

Nothing deferred — this is a complete, self-contained cleanup.

---
_Generated by [Claude Code](https://claude.ai/code)_
