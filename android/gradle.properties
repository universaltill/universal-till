android.useAndroidX=true
android.nonTransitiveRClass=true
org.gradle.jvmargs=-Xmx2048m
# AGP 8.7.3 was tested up to compileSdk 35; compileSdk 36 builds and runs
# correctly (verified on a real API 36 emulator) despite the warning —
# acknowledged deliberately rather than downgrading.
android.suppressUnsupportedCompileSdk=36
